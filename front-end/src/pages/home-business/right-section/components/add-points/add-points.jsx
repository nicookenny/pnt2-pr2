import { AddPointsSection } from '../../styled-components';
import { AddPointsForm } from './add-points-form';

export const AddPoints = () => {
  return (
    <AddPointsSection>
      <h2>Cargá puntos a tus clientes</h2>
      <AddPointsForm />
    </AddPointsSection>
  );
};
